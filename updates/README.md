# Private App Update Channel

Static signed updater files for Kirill's desktop apps.

- No GitHub Actions
- No GitHub Releases
- No Cloudflare
- No VPS/SSH
- macOS + Windows

Each app gets its own directory under `updates/<app-id>/` with signed platform artifacts and `latest.json` metadata.
